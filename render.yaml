services:
  - type: web
    name: fake-news-detecter
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python train_model.py --demo && streamlit run app.py --server.port $PORT --server.address 0.0.0.0
